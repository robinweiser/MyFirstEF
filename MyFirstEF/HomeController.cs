using System;
using System.Collections.Generic;
using System.Text;

namespace MyFirstEF
{
    /*
    public class HomeController
    {
        private readonly IMyProductService _myProductService;
        public HomeController(IMyProductService myProductService)
        {
            _myProductService = myProductService;
        }

        public string RunController(string name)
        {
            using (var scope = ApplicationServices.CreateScope())
            {


                var service = scope.ServiceProvider.GetService<IMyProductService>();
                service.AddLogging();
                service.createDatabase();
                //service.addData();
                service.ShowAllEntries();
                //service.FilterEntries();
                Console.ReadLine();
            }
        }
    }
    */
}
